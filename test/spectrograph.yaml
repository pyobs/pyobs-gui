# Test config for spectrographwidget
# Run with: pyobs spectrograph.yaml
class: pyobs.modules.MultiModule

modules:
  spectrograph:
    class: pyobs.modules.camera.DummySpectrograph
    comm:
      class: pyobs.comm.local.LocalComm
      name: spectrograph

  gui:
    class: pyobs_gui.GUI
    comm:
      class: pyobs.comm.local.LocalComm
      name: gui
